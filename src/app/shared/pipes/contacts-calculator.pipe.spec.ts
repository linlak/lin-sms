import { ContactsCalculatorPipe } from './contacts-calculator.pipe';

describe('ContactsCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactsCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
